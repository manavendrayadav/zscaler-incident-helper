# Web crawling
crawl4ai>=0.6.0
playwright>=1.45.0

# Vector database
qdrant-client>=1.12.0

# Embeddings (runs locally, no API cost)
sentence-transformers>=3.4.0
FlagEmbedding>=1.4.0          # bge-m3 hybrid dense+sparse embeddings
drain3>=0.9.11                # log template mining for extract_log_signals()

# LLM providers
openai>=1.0.0
groq>=0.14.0
anthropic>=0.30.0          # Anthropic Claude (claude-opus, claude-sonnet, claude-haiku)

# API
fastapi>=0.135.0
uvicorn[standard]>=0.44.0

# LangChain chunking
langchain>=0.3.0
langchain-community>=0.3.0
langchain-text-splitters>=0.3.0

# Utilities
pydantic>=2.12.0
python-dotenv>=1.2.0
httpx>=0.28.0
aiofiles>=24.0.0
tqdm>=4.0.0
rich>=15.0.0
requests>=2.31.0
