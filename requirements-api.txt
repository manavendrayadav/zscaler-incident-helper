# requirements-api.txt — used by Dockerfile only (rag-api container)
# Contains only what the API + embedding pipeline need.
# Does NOT include crawl4ai, playwright, patchright, beautifulsoup4 etc.
# (those are crawl-only packages installed on the host via requirements.txt)

# Vector database
qdrant-client>=1.14.0,<1.15.0

# Embeddings (runs locally, no API cost)
sentence-transformers>=5.6.0
FlagEmbedding>=1.4.0          # bge-m3 hybrid dense+sparse embeddings
drain3>=0.9.11                # log template mining for extract_log_signals()

# LLM providers
openai>=1.0.0
groq>=1.5.0
anthropic>=0.117.0

# API
fastapi>=0.135.0
uvicorn[standard]>=0.49.0

# LangChain chunking (text splitting used by API)
langchain>=0.3.0
langchain-text-splitters>=1.1.2

# Utilities
pydantic>=2.13.4
python-dotenv>=1.2.2
httpx>=0.28.0
aiofiles>=25.1.0
tqdm>=4.67.3
rich>=15.0.0
requests>=2.34.2
numpy>=2.5.1
